<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:wasted="clr-namespace:Wasted;assembly=Wasted"
             x:Class="Wasted.Pages.Place.NewPlacePage">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Add new place" Style="{StaticResource SectionTitle}"/>
            
            <Label Text="Title" />
            <wasted:RoundedEntry x:Name="TitleEntry"/>
            
            <Label Text="City" />
            <Picker x:Name="CityPicker" />
            
            <Label Text="{Binding MaxEntryLength, StringFormat='Description (fewer than {0} words)'}" />
            <wasted:RoundedEntry x:Name="DescriptionEntry" TextChanged="DescriptionEntryTextChanged"/>
            <Label>
                <Label.FormattedText>
                    <FormattedString>
                        <Span Text="{Binding EntryLength}" />
                        <Span Text="/" />
                        <Span Text="{Binding MaxEntryLength}" />
                    </FormattedString>
                    
                </Label.FormattedText>
            </Label>
            
            <Button Text="Next" Style="{StaticResource PurchaseButton}" Clicked="NextButtonClicked" VerticalOptions="EndAndExpand"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
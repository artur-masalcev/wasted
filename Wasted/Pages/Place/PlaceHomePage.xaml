<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Wasted.Pages.Place.PlaceHomePage">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Your places" />
            
            <!--#region Your places-->

            <CollectionView x:Name="YourPlacesCollectionView"
                            SelectionChanged="YourPlacesCollectionViewSelectionChanged"
                            SelectionMode="Single"
                            ItemsLayout="VerticalList"
                            ItemsSource="{Binding OwnedPlaces}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame>
                            <StackLayout Orientation="Horizontal">
                                <Label Text="{Binding Title }" />
                                <Label Text="{Binding City}" />
                                <Label Text="{Binding DealsCount, StringFormat='{0} deals'}" />
                                <Button x:Name="DeleteButton" Text="Delete" Command="{Binding Source={x:Reference YourPlacesCollectionView}}" CommandParameter="{Binding .}"></Button>
                            </StackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>

                
            </CollectionView>
            
            <Button Text="+" Clicked="AddNewPlaceButtonClicked"/>
            
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
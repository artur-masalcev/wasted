<?xml version="1.0" encoding="utf-8"?>

<local:HomePage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Wasted;assembly=Wasted"
             x:Class="Wasted.Pages.Place.PlaceHomePage">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Your places" />
            
            <!--#region Your places-->

            <CollectionView x:Name="YourPlacesCollectionView"
                            SelectionChanged="YourPlacesCollectionViewSelectionChanged"
                            ItemsLayout="VerticalList"
                            ItemsSource="{Binding OwnedPlaces}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame>
                            <StackLayout Orientation="Horizontal">
                                <Label Text="{Binding Title }" />
                                <Label Text="{Binding City}" />
                                <Label Text="{Binding DealsCount, StringFormat='{0} deals'}" />
                            </StackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
                
            </CollectionView>
            
        </StackLayout>
    </ContentPage.Content>
</local:HomePage>
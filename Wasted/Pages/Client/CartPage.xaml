<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
	xmlns="http://xamarin.com/schemas/2014/forms"
	xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
	x:Class="Wasted.CartPage">
	<ContentPage.Content>
        <RefreshView x:Name="RefreshView"  RefreshColor="DimGray" Refreshing="RefreshView_Refreshing">
	        <StackLayout>
		        <Label Text="Your cart:" Style="{StaticResource SectionTitle}"></Label>
		        <CollectionView x:Name="CartDealsCollectionView"
		                        SelectionMode="Single"
		                        ItemsLayout="VerticalList">
					<CollectionView.ItemTemplate>
				        <DataTemplate>
					        <Frame>
						        <Grid>
							        <Grid.ColumnDefinitions>
								        <ColumnDefinition Width="40"></ColumnDefinition>
								        <ColumnDefinition Width="*"></ColumnDefinition>
								        <ColumnDefinition Width="*"></ColumnDefinition>
							        </Grid.ColumnDefinitions>
							        <Label Grid.Column="0" Text="{Binding Quantity, StringFormat='{0} x' }"/>
							        <Label Grid.Column="1" Text="{Binding Title}" Margin="20,0, 0, 0"/>
							        <Label Grid.Column="2" Text="{Binding Cost, StringFormat='{0} €'}" HorizontalTextAlignment="End"/>
						        </Grid>
					        </Frame>
				        </DataTemplate>
			        </CollectionView.ItemTemplate>
		        </CollectionView>
		        <Label x:Name="Total" Style="{StaticResource SectionTitle}"/>
		        <Button Text="Purchase" Style="{StaticResource PurchaseButton}" Clicked="ClickedPurchase"/>
	        </StackLayout>
        </RefreshView>
    </ContentPage.Content>
</ContentPage>